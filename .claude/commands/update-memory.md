Update the project memory files in `instructions/memory/` to reflect what was accomplished in this session.

Do the following steps in order:

1. **Review session work:** Look at the conversation history to identify what was completed, what failed, and what was discovered.

2. **Update `instructions/memory/active_issues.md`:**
   - Add any new blocking issues discovered
   - Remove or mark resolved any issues that were fixed this session
   - Update severity/status of existing issues if they changed

3. **Update `instructions/memory/discoveries.md`:**
   - Add any new empirical findings (model results, statistical effects, effect sizes)
   - Add any new technical findings (pipeline fixes, data structure discoveries)
   - Organize by analysis stream

4. **Update `instructions/memory/open_questions.md`:**
   - Add new questions that arose this session
   - Remove or answer questions that were resolved
   - Keep organized by category (Theoretical, Methodological, Pipeline, Statistical)

5. **Update `instructions/memory/pipeline_state.md`:**
   - Update the ✅/❌/⚠️ status for every notebook and script touched this session
   - Update "Last updated" date at the top
   - Note any new output files created

6. **Update `instructions/memory/session_history.md`:**
   - Add a new section at the top for today's session (use today's date)
   - List under "Completed": everything that was successfully done
   - List under "Blocked / Not Completed": anything that was attempted but not finished, with reason
   - Keep each entry concise (1-2 lines max)

7. **Update `instructions/memory/MEMORY.md`** if any new memory files were created.

After updating, briefly summarize what was updated and what the most important open issues are going into the next session.
